version: "3"

services:
  app:
    volumes:
      - symfony4_core_app-sync:/app
  web:
    ports:
      - 8080:80
      - 8081:443
    volumes:
      - symfony4_core_app-sync:/app

volumes:
  symfony4_core_app-sync:
    external: true
  symfony4_core_web-sync:
    external: true
